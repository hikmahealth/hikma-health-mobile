export type User = {
  id: string;
  name: string;
  role: string;
  email: string;
  instance_url: string;
  phone: string;
};

export type NewUser = {
  id: string;
  name: string;
  role: string;
  email: string;
  phone: string;
  instance_url: string;
};
